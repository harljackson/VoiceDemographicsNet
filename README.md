# VoiceDemographicsNet
**VoiceDemographicsNet** is a machine learning project that predicts a speaker’s gender and age group from voice audio using MFCC features and a neural network. Built with Python, Librosa, and TensorFlow, the project uses the Mozilla Common Voice dataset and includes data preprocessing, model training, evaluation, and fairness considerations.
